{
  "name": "physics-capture",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev:banner && concurrently --kill-others-on-fail --prefix \"[{name}]\" -n \"signal,web,cv\" -c \"blue,green,yellow\" \"npm run dev --workspace=signaling\" \"npm run dev --workspace=frontend\" \".venv\\Scripts\\python packages/cv-service/grpc_server.py\"",
    "dev:banner": "node scripts/dev-banner.mjs",
    "dev:frontend": "npm run dev --workspace=frontend",
    "dev:signaling": "npm run dev --workspace=signaling",
    "dev:cv": ".venv\\Scripts\\python packages/cv-service/grpc_server.py",
    "test:frontend": "npm test --workspace=frontend",
    "test:cv": "pytest packages/cv-service/tests",
    "test:signaling": "npm test --workspace=signaling"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
